# Claude Code Notes

- Follow `AGENTS.md` for repository workflow.
- Never commit or push on your own. Leave all work uncommitted until the user gives an explicit command to commit and/or push.
- When explicitly told to commit and push, commit directly on `main` and `git push origin main`.
- Do not create or open pull requests unless explicitly requested.
